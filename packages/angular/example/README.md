# Angular Engine Example

This example demonstrates the usage of `@ldesign/engine-angular` with the unified `createEngineApp` API.

## Features

- âœ?Unified Engine API
- âœ?Plugin System
- âœ?Middleware Support
- âœ?Lifecycle Hooks
- âœ?State Management
- âœ?Event Bus


## Quick Start

\\\ash
# Install dependencies
pnpm install

# Start dev server
pnpm dev

# Build for production
pnpm build

# Preview production build
pnpm preview
\\\

## Usage

See `src/main.ts` for the complete example of how to use `createEngineApp` with Angular.

## API Reference

For more details, see the [@ldesign/engine-angular documentation](../../README.md).
